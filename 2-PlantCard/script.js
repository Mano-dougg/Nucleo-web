//getting the reference for the hamburguer tab
const optionList = document.getElementById("post-options__list")

//this function toggles the visibility of the tab using the "invisible" class
const toggleOptions = ()=>{
    optionList.classList.toggle("invisible")
}