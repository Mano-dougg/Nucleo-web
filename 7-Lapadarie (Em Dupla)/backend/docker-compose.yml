version: '4.3'
services:
  postgres:
    image: postgres
    container_name: postgres-container
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=24022022
      - POSTGRES_DB=lapadarie_db
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    container_name: api-container
    environment:
      - DATABASE_URL=postgresql://postgres:24022022@postgres:5432/lapadarie_db
    depends_on:
      - postgres
    ports:
      - "8000:8000"
    volumes:
      - .:/app

volumes:
  postgres-data:
